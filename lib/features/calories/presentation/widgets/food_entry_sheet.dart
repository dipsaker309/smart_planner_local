import 'package:flutter/material.dart';

class FoodEntrySheet extends StatelessWidget {
  const FoodEntrySheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.all(16),
      child: Text('Food entry form will be added later.'),
    );
  }
}
